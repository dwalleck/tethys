- Direct file dependency and dependent queries now resolve result paths in
  a single set-oriented query instead of one indexed-file lookup per
  returned file. Output and error behavior are unchanged.
