# Rust build artifacts
/target/
**/target/

# Cargo.lock for libraries (keep for binaries)
# We keep it since rivets is a binary application

# Debug files
**/*.pdb

# IDEs and Editors
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Cargo-husky user hooks (.cargo-husky/hooks/) ARE tracked — they are the
# source cargo-husky copies into .git/hooks on build. Do not ignore them.

# Beads left-side files (internal state)
.beads/beads.left.jsonl
.beads/beads.left.meta.json
.beads/metadata.json

# Python bytecode (probe scripts in audit-trail dirs)
__pycache__/

# OS files
Thumbs.db

# Git worktrees
.worktrees/

# Claude Code session settings (local) — but project skills are shared
.claude/*
!.claude/skills/

# Tethys SQLite index (regenerable from source)
.rivets/index/

# Private notes: details of non-public reference repositories used for
# qualification. Public docs and tracker entries refer to these files
# by path and never repeat their contents.
docs/private/
