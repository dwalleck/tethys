# tethys-j2r1 — tracker prior art (probe step 0)

- **tethys-aay4 + tethys-dl7l** (both CLOSED, PR #29) — the gate: parent
  linkage exists and impl identity is by the type field on both sides.
- **tethys-dvsw** (open) — dead code; the CONSUMER this feature exists for
  per the PRD ("promoted to Act 1 suppression infrastructure, scoped to
  that need"). Its AC references speculative-band suppression; hierarchy
  edges are the trait-impl suppression channel.
- **tethys-o4re** (open) — MCP tools (Act 2); the issue's MCP AC defers
  there, as with every analysis shipped this cycle.
- **tethys-0aqj** (open) — kind-blind binding; informs why C# method-level
  interface-member matching (name-only) is NOT attempted here.
- **tethys-xov3** (closed) — C# nested types indexed; base_list extraction
  builds on the same declaration walk.
- Deferral queue for this run: `.tethys-j2r1/to-file.md` (parallel session
  active — jsonl stays branch-clean; file at close-out).
