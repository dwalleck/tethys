{
  "path": "src/discovery/types.rs",
  "before": "    pub fn new(workspace_root: &Path, options: DiscoveryOptions) -> Result<Self> {\n        let workspace_root",
  "after": "    pub fn new(workspace_root: &Path, mut options: DiscoveryOptions) -> Result<Self> {\n        options.allow_restore = true;\n        let workspace_root",
  "tests": [
    "authorized_restore_rechecks_metadata_before_confirmation"
  ]
}
